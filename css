
<style>
@import url('https://fonts.googleapis.com/css?family=Clicker+Script|Elsie');
</style>


/*  This is a comment in CSS */

/* CSS Syntax

selector {
  property: value;
  property: value;
}
/*

/* Type selector */

p { /* will select all <p> elements */
  font-size: : 1em; /* px, pt % or em. px more universal across all browsers
  than pt. % or em % and em are relative ammounts. 1em = 16px */
  line-height: 1.5em;
}

figcaption {
  font-weight: normal;
  font-size: 0.75em;
  line-height: 1.5em;
  font-family: 'Elise', cursive;
  /* can write on one line: font: font-weight, font-size/line-height, font-family;*/
  color: rgba(136,	22, 22, 1	);
  text-align: right;
}

/*class selector can apply to as many elements as you like */

.milk-text { /* selects elements with a class of milk-text */
  font: bold 2.6em "Else", cursive;
  color: white;
  text-shadow: 0 2px 3px #000;
}

/* ID selector applies to one uinque element on a page */

#logo {
  background: aqua;
}

/* Descendent Selector */

#logo h1 { /* selects <h1> if inside #logo */
  font-family: "Clicker Script", cursive;
}

#logo h2 { /* selects <h2> if inside #logo */
  font-family: "Elsie", cursive;
}

/* Psuedo Selector */

#details a:link {
  color: black;
  text-decoration: none;
}

#details a:hover{
  color: black;
  text-decoration: underline;
}

#details a:active {
  color: red;
}
